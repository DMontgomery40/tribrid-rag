"""Reranker backends (learning/local/cloud) used by retrieval reranking.

This package is intentionally import-safe when optional ML backends are missing
(e.g. MLX). All optional dependencies must be imported lazily.
"""

