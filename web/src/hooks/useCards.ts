// Legacy alias: "cards" → chunk_summaries.
// Prefer `useChunkSummaries()`; keep `useCards()` for compatibility while migrating UI.
export { useChunkSummaries as useCards } from './useChunkSummaries';
export { default } from './useChunkSummaries';
