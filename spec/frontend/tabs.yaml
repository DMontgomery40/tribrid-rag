name: Tab Components
description: Top-level tab components
version: "1.0"

tabs:
  - name: StartTab
    route: /
    description: Dashboard and quick actions
    subtabs:
      - system-status
      - storage
      - monitoring
      - help
      - glossary

  - name: RAGTab
    route: /rag
    description: RAG configuration and indexing
    subtabs:
      - indexing
      - retrieval
      - reranker
      - learning
      - evaluate
      - data-quality

  - name: ChatTab
    route: /chat
    description: Conversational interface
    subtabs:
      - chat
      - settings

  - name: EvaluationTab
    route: /evaluation
    description: Run evaluations
    subtabs:
      - runner
      - dataset

  - name: EvalAnalysisTab
    route: /analysis
    description: Analyze evaluation results
    subtabs:
      - history
      - drilldown
      - traces

  - name: GrafanaTab
    route: /grafana
    description: Observability dashboards
    subtabs:
      - dashboard
      - config

  - name: GraphTab
    route: /graph
    description: Knowledge graph exploration
    subtabs:
      - explorer
      - communities
      - config

  - name: InfrastructureTab
    route: /infrastructure
    description: Infrastructure management
    subtabs:
      - docker
      - services
      - paths

  - name: AdminTab
    route: /admin
    description: Admin settings
    subtabs:
      - general
