name: Grafana Components
description: Grafana integration components
version: "1.0"

components:
  - name: GrafanaDashboard
    props:
      - name: dashboardId
        type: string
        required: false

  - name: GrafanaConfig
    props: []

  - name: GrafanaSubtabs
    props: []
